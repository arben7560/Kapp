export async function generateLipSync(_audioUrl?: string): Promise<string | undefined> {
  // V1 : à connecter plus tard à D-ID, HeyGen, Tavus, etc.
  // Retour attendu : URL du stream ou de la vidéo avatar générée.
  return undefined;
}
